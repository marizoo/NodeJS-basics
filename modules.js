// const getppl = require("./people");
//u can destructure the above example.
const { people, ages } = require("./people");

// console.log(getppl);
console.log(people, ages);
//to get the output separately

//OS OS OS
const os = require("os");
console.log(os);

console.log(os.platform(), os.homedir());
